services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - '3001:8080'

  web:
    build:
      context: ..
      dockerfile: infra/Dockerfile.web
    ports:
      - '8080:80'
    depends_on:
      - api
